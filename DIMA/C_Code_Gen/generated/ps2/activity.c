#include <os/pos/apex/apexLib.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "activity.h"
#include "gtypes.h"
#include "deployment.h"
#include "globals.h"
#include "subprograms.h"

/*****************************************************/
/*  This file was automatically generated by aadl2c */
/*  Partition: ps2  AADL: P2.impl              */
/*  DO NOT hand-modify: changes will be overwritten  */
/*****************************************************/


#define SZ 1024

/* ================================================================== */
/*  task21  (AADL: Tsk21)
 *  Period=50ms  Priority=2  Dispatch_Abs=0ms
 *  Inter-partition READS:  pr2samplingin (SAMPLING DEST)
 *  Intra-partition WRITES (BB):  bb_task1to2BlackBoard
 *  Intra-partition READS  (BUF): buf_task2to1Buffer
 * ================================================================== */
extern BLACKBOARD_ID_TYPE ps2_bb_task1to2BlackBoard_id;
extern BUFFER_ID_TYPE ps2_buf_task2to1Buffer_id;
extern SAMPLING_PORT_ID_TYPE ps2_pr2samplingin_id;

void* task21_job(void)
{
    RETURN_CODE_TYPE ret = NO_ERROR;
    VALIDITY_TYPE    validity;
    MESSAGE_SIZE_TYPE msg_len;
    integer val = 0;

    while (1)
    {
        /* READ inter-partition SAMPLING: pr2samplingin */
        READ_SAMPLING_MESSAGE(ps2_pr2samplingin_id,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &validity, &ret);
        CHECK_VALIDCODE("READ_SAMPLING_MESSAGE(pr2samplingin)", validity, ret);

        /* READ intra-partition BUFFER: buf_task2to1Buffer */
        READ_BUFFER(ps2_buf_task2to1Buffer_id, 5000000ll,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &ret);
        CHECK_CODE("READ_BUFFER(buf_task2to1Buffer)", ret);

        commandboard_receiveinput_spg(&val, &val);  /* user subprogram */
        commandboard_printinfos_spg(&val, &val);  /* user subprogram */

        /* DISPLAY intra-partition BLACKBOARD: bb_task1to2BlackBoard */
        DISPLAY_BLACKBOARD(ps2_bb_task1to2BlackBoard_id,
            (MESSAGE_ADDR_TYPE)&val, sizeof(integer), &ret);
        CHECK_CODE("DISPLAY_BLACKBOARD(bb_task1to2BlackBoard)", ret);

        PERIODIC_WAIT(&ret);
        CHECK_CODE("PERIODIC_WAIT(task21)", ret);
    }
}

/* ================================================================== */
/*  task22  (AADL: Tsk22)
 *  Period=50ms  Priority=3  Dispatch_Abs=2ms
 *  Inter-partition READS:  pr2samplingin (SAMPLING DEST)
 *  Intra-partition READS  (BB):  bb_task1to2BlackBoard
 *  Intra-partition WRITES (BUF): buf_task2to1Buffer, buf_task2to3buffer, buf_task2to3blackboard
 *  Intra-partition READS  (BUF): buf_task3to2buffer, buf_task3to2blackboard
 * ================================================================== */
extern BLACKBOARD_ID_TYPE ps2_bb_task1to2BlackBoard_id;
extern BUFFER_ID_TYPE ps2_buf_task2to1Buffer_id;
extern BUFFER_ID_TYPE ps2_buf_task2to3blackboard_id;
extern BUFFER_ID_TYPE ps2_buf_task2to3buffer_id;
extern BUFFER_ID_TYPE ps2_buf_task3to2blackboard_id;
extern BUFFER_ID_TYPE ps2_buf_task3to2buffer_id;
extern SAMPLING_PORT_ID_TYPE ps2_pr2samplingin_id;

void* task22_job(void)
{
    RETURN_CODE_TYPE ret = NO_ERROR;
    VALIDITY_TYPE    validity;
    MESSAGE_SIZE_TYPE msg_len;
    integer val = 0;

    while (1)
    {
        /* READ inter-partition SAMPLING: pr2samplingin */
        READ_SAMPLING_MESSAGE(ps2_pr2samplingin_id,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &validity, &ret);
        CHECK_VALIDCODE("READ_SAMPLING_MESSAGE(pr2samplingin)", validity, ret);

        /* READ intra-partition BLACKBOARD: bb_task1to2BlackBoard */
        READ_BLACKBOARD(ps2_bb_task1to2BlackBoard_id, 0ll,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &ret);
        CHECK_CODE("READ_BLACKBOARD(bb_task1to2BlackBoard)", ret);

        /* READ intra-partition BUFFER: buf_task3to2buffer */
        READ_BUFFER(ps2_buf_task3to2buffer_id, 5000000ll,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &ret);
        CHECK_CODE("READ_BUFFER(buf_task3to2buffer)", ret);

        /* READ intra-partition BUFFER: buf_task3to2blackboard */
        READ_BUFFER(ps2_buf_task3to2blackboard_id, 5000000ll,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &ret);
        CHECK_CODE("READ_BUFFER(buf_task3to2blackboard)", ret);

        /* WRITE intra-partition BUFFER: buf_task2to1Buffer */
        WRITE_BUFFER(ps2_buf_task2to1Buffer_id,
            (MESSAGE_ADDR_TYPE)&val, sizeof(integer),
            5000000ll, &ret);
        CHECK_CODE("WRITE_BUFFER(buf_task2to1Buffer)", ret);

        /* WRITE intra-partition BUFFER: buf_task2to3buffer */
        WRITE_BUFFER(ps2_buf_task2to3buffer_id,
            (MESSAGE_ADDR_TYPE)&val, sizeof(integer),
            5000000ll, &ret);
        CHECK_CODE("WRITE_BUFFER(buf_task2to3buffer)", ret);

        /* WRITE intra-partition BUFFER: buf_task2to3blackboard */
        WRITE_BUFFER(ps2_buf_task2to3blackboard_id,
            (MESSAGE_ADDR_TYPE)&val, sizeof(integer),
            5000000ll, &ret);
        CHECK_CODE("WRITE_BUFFER(buf_task2to3blackboard)", ret);

        PERIODIC_WAIT(&ret);
        CHECK_CODE("PERIODIC_WAIT(task22)", ret);
    }
}

/* ================================================================== */
/*  task23  (AADL: Tsk23)
 *  Period=100ms  Priority=4  Dispatch_Abs=0ms
 *  Inter-partition READS:  pr2samplingin (SAMPLING DEST)
 *  Inter-partition WRITES: pr2queueingout (QUEUING SOURCE)
 *  Intra-partition WRITES (BUF): buf_task3to2buffer, buf_task3to2blackboard
 *  Intra-partition READS  (BUF): buf_task2to3buffer, buf_task2to3blackboard
 * ================================================================== */
extern BUFFER_ID_TYPE ps2_buf_task2to3blackboard_id;
extern BUFFER_ID_TYPE ps2_buf_task2to3buffer_id;
extern BUFFER_ID_TYPE ps2_buf_task3to2blackboard_id;
extern BUFFER_ID_TYPE ps2_buf_task3to2buffer_id;
extern QUEUING_PORT_ID_TYPE ps2_pr2queueingout_id;
extern SAMPLING_PORT_ID_TYPE ps2_pr2samplingin_id;

void* task23_job(void)
{
    RETURN_CODE_TYPE ret = NO_ERROR;
    VALIDITY_TYPE    validity;
    MESSAGE_SIZE_TYPE msg_len;
    integer val = 0;

    while (1)
    {
        /* READ inter-partition SAMPLING: pr2samplingin */
        READ_SAMPLING_MESSAGE(ps2_pr2samplingin_id,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &validity, &ret);
        CHECK_VALIDCODE("READ_SAMPLING_MESSAGE(pr2samplingin)", validity, ret);

        /* READ intra-partition BUFFER: buf_task2to3buffer */
        READ_BUFFER(ps2_buf_task2to3buffer_id, 5000000ll,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &ret);
        CHECK_CODE("READ_BUFFER(buf_task2to3buffer)", ret);

        /* READ intra-partition BUFFER: buf_task2to3blackboard */
        READ_BUFFER(ps2_buf_task2to3blackboard_id, 5000000ll,
            (MESSAGE_ADDR_TYPE)&val, &msg_len, &ret);
        CHECK_CODE("READ_BUFFER(buf_task2to3blackboard)", ret);

        commandboard_receiveinput_spg(&val, &val);  /* user subprogram */
        commandboard_printinfos_spg(&val, &val);  /* user subprogram */

        /* SEND inter-partition QUEUING: pr2queueingout */
        SEND_QUEUING_MESSAGE(ps2_pr2queueingout_id,
            (MESSAGE_ADDR_TYPE)&val, sizeof(integer),
            5ll, &ret);
        CHECK_CODE("SEND_QUEUING_MESSAGE(pr2queueingout)", ret);

        /* WRITE intra-partition BUFFER: buf_task3to2buffer */
        WRITE_BUFFER(ps2_buf_task3to2buffer_id,
            (MESSAGE_ADDR_TYPE)&val, sizeof(integer),
            5000000ll, &ret);
        CHECK_CODE("WRITE_BUFFER(buf_task3to2buffer)", ret);

        /* WRITE intra-partition BUFFER: buf_task3to2blackboard */
        WRITE_BUFFER(ps2_buf_task3to2blackboard_id,
            (MESSAGE_ADDR_TYPE)&val, sizeof(integer),
            5000000ll, &ret);
        CHECK_CODE("WRITE_BUFFER(buf_task3to2blackboard)", ret);

        PERIODIC_WAIT(&ret);
        CHECK_CODE("PERIODIC_WAIT(task23)", ret);
    }
}

