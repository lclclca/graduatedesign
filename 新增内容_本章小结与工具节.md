# 新增内容：本章小结（2/3/4章）+ §4.6 工具设计与实现

本文件包含四处需要插入 thesis_revised.md 的内容，以及一处移动操作（§4.2 移至末尾）。
每节均标明**精确的操作位置**和**完整的插入文本**。

---

## 操作总览

| 序号 | 操作类型 | 插入/操作位置 | 标志行（thesis_revised.md） |
|------|----------|-------------|---------------------------|
| ①   | 插入     | 第二章末尾   | 第 209 行 `---`（第二章与第三章之间的分隔线）**之前** |
| ②   | 插入     | 第三章末尾   | 第 500 行 `---`（第三章与第四章之间的分隔线）**之前** |
| ③   | 移动     | §4.2 → §4.6 | 将第 594–679 行（`## 4.2 评估工具实现`…至 `## 4.3`之前）整体**剪切**，贴到第四章末尾（§4.5 之后，分隔线 `---` 之前），同时更新节号 |
| ④   | 插入     | 第四章末尾   | 移动后的 §4.6 **之后**，第 898 行 `---` **之前** |

---

## ① 第二章 本章小结

**操作**：在 thesis_revised.md 第 209 行（`---` 分隔线）之前插入以下内容。

```
### 2.4.3 结构化输入对代码生成的影响
```
这一节结束后（即"减少解析错误"句号后），在第209行 `---` 前插入：

---

```markdown
## 2.5 本章小结

本章系统梳理了本文研究所涉及的四个核心背景领域，为后续方法设计与实验分析提供理论支撑。

在 ARINC 653 标准方面，本章介绍了分区模型的时空隔离机制、APEX 服务接口的 API 分类与调用规范（进程管理、分区间/分区内通信、模式管理），明确了健康监控回调（`module_HM_callback`/`partition_HM_callback`）的强制实现要求，以及 `CHECK_CODE` 宏在工程实践中的规范用法。这些约束共同构成评估生成代码是否合规的检查基准。

在综合模块化航空电子系统方面，本章阐明了基于 ACoreOS653 实时操作系统的 11 文件代码结构，说明了每类文件的功能定位（配置常量、类型定义、全局宏、子程序、任务函数、初始化入口），为自动生成的目标格式提供了明确参照。

在 AADL 架构描述语言方面，本章说明了 `process`/`thread`/`properties` 等关键语法元素与分区设计信息的对应关系，明确了 AADL 模型作为代码生成输入源所携带的语义信息范围——任务数量与参数、端口类型与方向、资源数量与命名。

在大语言模型与提示工程方面，本章介绍了当前主流 LLM 的代码生成能力基线，梳理了零样本、思维链、少样本、组合四类提示策略的基本原理与适用场景，并分析了结构化输入相比自然语言/形式化语言输入在减少解析歧义方面的价值。上述内容为第三章提示词模板的领域针对性设计提供了问题背景。
```

---

## ② 第三章 本章小结

**操作**：在 thesis_revised.md 第 500 行（`---` 分隔线）之前插入以下内容。

第 498 行末尾为"各策略的代码质量量化评估结果详见第四章。"，在其后、第 500 行 `---` 之前插入：

---

```markdown
## 3.6 本章小结

本章提出并详细描述了基于提示词模板的 IMA 代码自动生成方法，涵盖方法设计、规格表示、实验系统构建和提示词模板设计四个方面。

在方法架构方面，本章确立了"AADL解析 → JSON规格 → 提示词填充 → LLM生成 → 代码评估"的三阶段流水线。以结构化 JSON 作为 AADL 与 LLM 之间的中间表示层，是该方法的核心设计决策：JSON 去除了 AADL 形式化语法中的噪声，以扁平键值结构精确传递生成代码所需的全部语义信息，有效降低了模型的解析负担，并经实验初步验证了其对周期纳秒值、端口数量宏等精确数值生成质量的正向作用。

在规格设计方面，本章确立了包含 `partition`、`tasks[]`、通信端口数组、内部资源名称数组和 `deployment` 宏值对象的 JSON schema，并说明了 `aadl2c.py` 脚本在线程属性提取（含单位规范化）、端口方向识别和连接关系推断三个关键步骤上的正则解析实现。

在实验系统方面，本章构建了 DIMA（5 分区/12 任务）、IMA2（3 分区/6 任务）和 IMA3（4 分区/10 任务）三套完整 IMA 测试系统及其 AADL 模型和规格 JSON，覆盖从最简（1 任务/1 端口）到最复杂（4 任务/全资源类型）的分区配置，为多维度对比实验提供了受控的测试环境。

在提示词模板方面，本章面向 ARINC 653 代码生成的三类领域难点（API 语义密集/平台隐式约定/11 文件固定骨架）逆向设计了四种提示词模板：零样本模板以显式文件清单覆盖骨架难点；思维链模板以四步推理链和嵌入 API 参考覆盖语义密集难点；少样本模板以 ps3 完整代码示例覆盖隐式约定难点；组合模板通过示例 + 推理链 + API 片段实现三类难点的协同覆盖。各模板的代码质量量化评估结果详见第四章。
```

---

## ③ §4.2 移至末尾并重命名为 §4.6

**操作**：将 thesis_revised.md 第 594–679 行（`## 4.2 评估工具实现` 至 `## 4.3` 之前）整体**剪切**，在 §4.5 正文结束处（第 898 行 `---` **之前**）粘贴，同时作如下文本替换：

| 原文本（在被移动的块中） | 替换为 |
|------------------------|--------|
| `## 4.2 评估工具实现` | `## 4.6 工具设计与实现` |
| `### 4.2.1 核心评估模块` | `### 4.6.1 核心评估模块` |
| `### 4.2.2 命令行脚本` | `### 4.6.2 命令行脚本` |
| `### 4.2.3 图形化评估界面` | `### 4.6.3 图形化评估界面` |
| `整体架构如图4-1所示。` | `整体架构如图4-5所示。` |
| `【图4-1：评估工具架构图 — 待插入】` | `【图4-5：评估工具架构图 — 待插入】` |
| `【图4-2：单分区评估界面截图 — 待插入…】` | `【图4-6：单分区评估界面截图 — 待插入…】` |
| `【图4-3：ARINC 653合规检查界面截图 — 待插入…】` | `【图4-7：ARINC 653合规检查界面截图 — 待插入…】` |

移动后，原第 594–679 行处应只剩下 `## 4.3 三维评分实验结果` 直接接在 `## 4.1 评估框架设计` 之后（注意保留原有的段落过渡；如需要，可在 §4.1 末尾补一句"评估工具的具体实现详见本章 §4.6"）。

---

## ④ 第四章 本章小结

**操作**：在移动后的 §4.6 最后一段之后、第 898 行 `---` 之前插入：

---

```markdown
## 4.7 本章小结

本章构建了面向 ARINC 653 代码生成质量的双层分层评估体系，并基于该体系对四种提示策略展开了系统性实验验证，最后介绍了评估框架的完整工具链实现。

在评估框架方面，本章提出了三维评分框架（结构完整性 S×30%、API正确性 A×40%、语义一致性 C×30%，共 30 个检查项）和 ARINC 653 合规检查模块（R1–R8 共 8 条规则）。两个模块的互补关系通过少样本/PB 分区"黑板创建但未在任务函数中调用"的典型案例得到了具体验证：三维评分无法识别这一语义级别的遗漏，而合规检查的 R1 规则则直接检出。

在主实验结果方面（16 组，DIMA + IMA2 系统），四种策略呈现出清晰的梯度分布：组合策略在三维综合得分（100.0）和 ARINC 653 合规率（94.4%）两个层次均表现最优，适合作为工程实践的首选；少样本策略三维满分、合规率 91.7%，与组合策略相当；零样本策略三维均值 95.4 但合规率较低（72.2%），说明仅凭模型知识难以覆盖双端 API 的配对语义；思维链策略因辅助文件格式的系统性失分（S7/S8 全部分区失分）导致三维均值降至 86.4，低于零样本。

在推广性验证方面（8 组，IMA3 系统），少样本和组合策略在目标分区任务数从 6 扩展至 10、最复杂分区（NAV）包含 4 任务和全类型资源的条件下，三维评分仍取得 100.0 满分，合规率与 IMA2 实验差值均在 2 个百分点以内，验证了两种策略稳健的跨系统泛化能力。

在工具实现方面，本章介绍了评估框架的 Python 实现，包括封装四类检查规则的 `metrics/` 核心模块（统一接口 `check(gen_dir, spec)`）、支持单分区评估和批量合规扫描的命令行脚本，以及基于 `tkinter+ttk` 实现的三标签页图形化界面，三个层次共享同一套计算逻辑，确保结果一致性，为研究可重现性提供工程保障。
```

---

## 修改位置速查表

```
thesis_revised.md 行号（修改前）:

  第 209 行  ---        ← ① 在此行之前插入 §2.5 本章小结
  第 500 行  ---        ← ② 在此行之前插入 §3.6 本章小结
  第 594 行  ## 4.2 …  ← ③-A 从此行开始剪切（含4.2.1/4.2.2/4.2.3，共约86行）
  第 680 行  ## 4.3 …  ← ③-B 剪切结束于此行之前
  第 898 行  ---        ← ③-C 在此行之前粘贴 §4.6（重命名后），然后
                           ④ 紧接其后、此行之前插入 §4.7 本章小结
```

**注意**：③ 剪切完成后，原 §4.3/4.4/4.5 的编号不需要变动（仍为 4.3/4.4/4.5），新增的工具节为 §4.6，本章小结为 §4.7。

---

*本文件由 Claude 生成，仅供插入参考，不直接修改 thesis_revised.md。*
